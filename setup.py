from pyquestionnaire import __version__
from setuptools import setup, find_packages

setup(
    name='pyquestionnaire',
    packages=find_packages(),
    include_package_data=True,
    version=__version__,
    install_requires=[]
)
